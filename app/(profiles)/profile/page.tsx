import OwnCompanyProfile from "../company/page"

export default function OwnProfile() {

    if (false) // WENN currentUser.company.email == currentUser.email
        return <OwnCompanyProfile />

    // ONLY OWN PROFILE

    return (
        <div>Profile</div>
    )
}
